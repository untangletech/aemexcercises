var chai = require('chai');
var expect = chai.expect;

describe("karma should run for multiple files", function() {
    it('karma should run for multiple files', function() {
        expect(true).to.be.true;
    });
});