var chai = require('chai');
var expect = chai.expect;

describe("Country Selector", function() {
    it('should test number list module', function() {
        expect(true).to.be.true;
    });
});